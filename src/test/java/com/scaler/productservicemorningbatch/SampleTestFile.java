package com.scaler.productservicemorningbatch;

import com.scaler.productservicemorningbatch.controllers.ProductController;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.assertEquals;


//@SpringBootTest
public class SampleTestFile {
//    @Autowired
//    private ProductController productController;
//
//    @Test
//    void testOnePlusOneIsTwo() {
//        //3 A's
//        // Arrange, Act and Assert.
//
//        int i = 1 + 1; //Arrange + Act.
//
//        assert i == 2;
//    }
//
//    @Test
//    void testMultiplyFunction() {
//        int x = 2 * 2;
//        int y = 3 * 3;
//
//        assert x == 4;
//
////        assert y == 7;
//        //assertEquals()
//        assertEquals(9, y, "3*3 isn't returning correct output.");
////        assertNull(product);
//
//
////        assertThrows(
////                InvalidProductIdException.class,
////                () -> productController.getProductById(10000L)
////        );
//
//        //assertFalse();
//    }
}

/*
Arrange - Create the variables/objects required for testing.
Act - Call the required functions.
Assert - Check the function output against the expected output.

We can have any number of assert conditions in the test case, and the
test case will pass iff all the assert statement passes.

In the assert function, the first value should be the expected, followed by actual value.
 */
